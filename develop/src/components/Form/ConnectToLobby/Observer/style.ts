import styled from 'styled-components';
import { FlexBox } from '../../../../shared/FlexBox';

export const ObserverBox = styled(FlexBox)`
  position: absolute;
  top: 75px;
  right: 70px;

  .text {
    font-family: Ruda;
  }
`;
